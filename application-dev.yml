spring:
  application:
    name: Config-dev
eureka:
  instance:
    prefer-ip-address: true
  cliente:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
